# Chroma-server
:book:

```
// How to run this project 

// start mongoDB locally:

mongod --dbpath ~/PATH/TO/DB

// start server

npm start

// load small test json data to db:

npm run load-data

```
